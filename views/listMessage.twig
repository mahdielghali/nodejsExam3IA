<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <table>
        <tr>
        <th>Pseudo</th>
        <th>Content</th>
        <th>Likes</th>
        <th>Actions</th>
    </tr>
        {% for message in messages %}
    <tr>
            <td>{{message.pseudo}}</td>
            <td>{{message.content}}</td>
            <td>{{message.likes}}</td>
            <td><div>
                <a href='/like/{{message._id}}'>like</a>
                <a href="/updateMessage/{{message._id}}">edit</a>
                <a href="/deleteMessage/{{message._id}}">delete</a>
            </div></td>
        </tr>
        {% endfor %}
    </table>
    
</body>
</html>